version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - $PORT:$PORT

  nginx:
    image: nginx
    volumes:
      - ./docker/jsbot.conf.template:/etc/nginx/templates/jsbot.conf.template
    ports:
      - "80:80"
    environment:
      - NGINX_PORT=$PORT
    depends_on:
      - app
